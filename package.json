{
  "name": "gmaps-martin",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mocha --compilers js:babel-core/register",
    "build": "browserify src/app.js --outfile dist/index.js --transform=babelify",
    "watch:build": "watch 'npm run build' src",
    "server": "http-server"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "babel-core": "^5.6.15",
    "babelify": "^6.1.2",
    "browserify": "^10.2.4",
    "http-server": "^0.8.0",
    "mocha": "^2.2.5",
    "sinon": "^1.15.4",
    "watch": "^0.16.0"
  },
  "dependencies": {
    "leaflet": "^0.7.3"
  }
}
